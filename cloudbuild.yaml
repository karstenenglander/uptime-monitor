steps:
  # Build the container image
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "build",
        "-t",
        "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${COMMIT_SHA}",
        ".",
      ]

  # Push the container image to Artifact Registry
  - name: "gcr.io/cloud-builders/docker"
    args:
      [
        "push",
        "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${COMMIT_SHA}",
      ]

  # Build secrets
  - name: "bash"
    secretEnv:
      - TERRAFORM_TFVARS
    entrypoint: bash
    args:
      - -c
      - echo "$TERRAFORM_TFVARS" > /workspace/terraform.tfvars

  # Terraform steps
  - name: "hashicorp/terraform:latest"
    args: ["init", "-backend-config=backend.hcl"]
  - name: "hashicorp/terraform:latest"
    args:
      [
        "plan",
        "-var-file=terraform.tfvars",
        "-var",
        "cloud_build_image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${COMMIT_SHA}",
      ]
  - name: "hashicorp/terraform:latest"
    args:
      [
        "apply",
        "-auto-approve",
        "-var-file=terraform.tfvars",
        "-var",
        "cloud_build_image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${COMMIT_SHA}",
      ]

    # Goose Migrations
  - id: install-proxy
    name: gcr.io/cloud-builders/wget
    entrypoint: sh
    args:
      - -c
      - |
        wget -O /workspace/cloud-sql-proxy https://storage.googleapis.com/cloud-sql-connectors/cloud-sql-proxy/v2.22.1/cloud-sql-proxy.linux.amd64
        chmod +x /workspace/cloud-sql-proxy

  - id: migrate
    waitFor: ["install-proxy"]
    name: golang:1.26
    entrypoint: sh
    env:
      - "DATABASE_NAME=${_DATABASE_NAME}"
      - "DATABASE_PORT=${_DATABASE_PORT}"
      - "INSTANCE_CONNECTION_NAME=${_INSTANCE_CONNECTION_NAME}"
    secretEnv:
      - POSTGRES_PASSWORD
    args:
      - "-c"
      - |
        apt-get update && apt-get install -y postgresql-client
        /workspace/cloud-sql-proxy ${_INSTANCE_CONNECTION_NAME} --port ${_DATABASE_PORT} & until pg_isready -h 127.0.0.1 -p 5432; do sleep 1; done
        # Cloud SQL Proxy is now up and running, add your own logic below to connect
        go install github.com/pressly/goose/v3/cmd/goose@latest
        $(go env GOPATH)/bin/goose -dir /workspace/internal/adapters/postgresql/migrations postgres "host=127.0.0.1 port=5432 user=migration_iam_user password=$$POSTGRES_PASSWORD dbname=${_DATABASE_NAME} sslmode=disable" up

substitutions:
  _DATABASE_NAME: uptime-database
  _INSTANCE_CONNECTION_NAME: englander-suite:us-east4:uptime-database-instance
  _DATABASE_PORT: "5432"
  _SERVICE: uptime-monitor-gcr
  _REGION: us-east4
  _REPO: docker-artifact-repository

options:
  logging: CLOUD_LOGGING_ONLY

availableSecrets:
  secretManager:
    - versionName: projects/${PROJECT_ID}/secrets/POSTGRES_PASSWORD/versions/latest
      env: POSTGRES_PASSWORD
    - versionName: projects/${PROJECT_ID}/secrets/TERRAFORM_TFVARS/versions/latest
      env: TERRAFORM_TFVARS

images:
  - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_REPO}/${_SERVICE}:${COMMIT_SHA}"
